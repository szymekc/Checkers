using System;
using System.Collections.Generic;
using System.Text;

namespace Checkers {
    class Human : Player {
        public Human(Color color, Board board) : base(color, board) {
        }
    }
}
